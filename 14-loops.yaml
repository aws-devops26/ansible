- name: loops
  hosts: localhost
  tasks:
  - name: install or uninstall
    ansible.builtin.package:
    name: "{{item}}"
    state: present # item is a default keyboard
  loop:
  - ansible
  - git
  - mysql
  - postfix
